<%= simple_form_for(@administrar_usuario) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :rol %>
    <%= f.association :user %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
